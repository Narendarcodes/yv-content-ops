# ---- Server ----
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1
LOG_LEVEL=info

# ---- MongoDB ----
# Local dev (docker compose up -d mongo) or any MongoDB URI
MONGO_URI=mongodb://127.0.0.1:27017/cop

# ---- JWT ----
# Access-token secret. CHANGE THIS in any real environment.
# The app refuses to start in production if this is left as a known default.
JWT_ACCESS_SECRET=change-me
JWT_ACCESS_EXPIRES_IN=15m
# Refresh tokens are stored hashed (SHA-256) in the DB and expire after this many days
JWT_REFRESH_EXPIRES_DAYS=7

# ---- CORS ----
# Comma-separated list of allowed origins. Empty/omitted = allow any origin (dev only).
# Set this in production, e.g. CORS_ORIGIN=https://app.example.com,https://admin.example.com
# CORS_ORIGIN=

# ---- Storage (file uploads) ----
# 'local' writes uploads to STORAGE_LOCAL_DIR. Swap in a cloud adapter later.
STORAGE_DRIVER=local
STORAGE_LOCAL_DIR=./uploads

# ---- Rate limiting (global per-IP) ----
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
